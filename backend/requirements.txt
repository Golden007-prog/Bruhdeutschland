# DeutschPrep backend — core API runtime (CLAUDE.md §3).
# ML/embedding + fine-tuning extras live in requirements-embeddings.txt (GPU-only, never in CI).
# Versions are pinned to what the dev venv resolved; psycopg/redis (Docker-only) use floors.

# Web framework
fastapi==0.137.2
uvicorn[standard]==0.49.0
python-multipart==0.0.32

# LLM
anthropic==0.109.2

# Data / validation / config
pydantic==2.13.4
pydantic-settings==2.14.1

# Persistence
SQLAlchemy==2.0.51
alembic==1.18.4
pgvector==0.4.2
psycopg[binary]>=3.2

# Cache / queue
redis>=5.2

# HTTP + parsing + logging
httpx==0.28.1
selectolax==0.4.10
pypdf==6.13.3
python-docx==1.2.0
structlog==26.1.0

# Dev / CI (lint, type, test)
ruff==0.15.17
black==26.5.1
mypy==2.1.0
pytest==9.1.0
